# Utilities

Pure helpers (formatters, date utilities) shared across features. Provide Vitest coverage.
- `security/pathGuard.ts` keeps all generated file-system paths within the app sandbox before they reach Tauri commands.
